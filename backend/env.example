# MongoDB Connection
# For local development, use: mongodb://localhost:27017/onlyswap
# For MongoDB Atlas (cloud), use your connection string from Atlas dashboard
MONGO_URI=mongodb://localhost:27017/onlyswap

# JWT Secret - Generate a random string for production
# You can generate one with: openssl rand -base64 32
# IMPORTANT: Must be at least 32 characters long in production!
JWT_SECRET=supersecretkey

# Server Configuration
PORT=3001
HOST=0.0.0.0

# Environment (development, production, test)
NODE_ENV=development

# Logging
# Options: error, warn, info, http, debug
LOG_LEVEL=info

# CORS Configuration (for production)
# Comma-separated list of allowed origins
# Example: ALLOWED_ORIGINS=https://yourapp.com,https://www.yourapp.com
ALLOWED_ORIGINS=*

# Email Configuration (for verification emails)
# Option 1: Resend API (RECOMMENDED - Free, reliable, won't go to spam)
# Get free API key from: https://resend.com/api-keys
# Free tier: 3,000 emails/month, great deliverability for .edu emails
RESEND_API_KEY=re_your_resend_api_key_here

# Option 2: Gmail SMTP (Fallback)
# Get Gmail App Password from: https://myaccount.google.com/apppasswords
GMAIL_USER=onlyswapwck@gmail.com
GMAIL_APP_PASSWORD=your_gmail_app_password_here

# Option 3: SendGrid API (Fallback)
# Get API Key from: https://app.sendgrid.com/
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Cloudinary Configuration (for image storage)
# Get these from https://cloudinary.com/console
# If not set, images will be stored locally (requires Railway volume for persistence)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
